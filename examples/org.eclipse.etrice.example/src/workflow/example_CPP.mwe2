module workflow.etrice.example_CPP

import org.eclipse.emf.mwe.utils.*

var destDir = "src-gen"

Workflow {
	
	component = DirectoryCleaner {
		directory = destDir
	}

	component = @workflow.RoomGenerator_CPP {
		sourceDir = "models"
		targetDir = destDir
	}
}